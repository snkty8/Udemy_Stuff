let myBook = {
    title: '1984',
    author: 'George Orwell',
    pageCount: 326
}

console.log(`${myBook.title} by ${myBook.author}`)

// Change the title of the book
myBook.title = 'Animal Farm'
console.log(`${myBook.title} by ${myBook.author}`)


// Challenge Area
// name
// age
// location
// then generate a string person is 27 and lives in a place
// then increase age

let person = {
    Name: 'Sierra',
    Age: 36,
    location: 'Jacksonville'
}

console.log(`${person.Name} is ${person.Age} years old, and lives in ${person.location}.`)

person.Age = 37
console.log(`${person.Name} is ${person.Age} years old, and lives in ${person.location}.`)